"""Agents-private SQLAlchemy models.

NOTE: dry_run.py se importa explícitamente en
``backend/app/models/__init__.py`` para registro metadata global.
NO re-export aquí · evita circular import (Base ← models/__init__).
"""
